# generated by octocov init
coverage:
  if: true
codeToTestRatio:
  code:
    - '**/*.go'
    - '!**/*_test.go'
  test:
    - '**/*_test.go'
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://sun-yryr/Rec-adio
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: github.event_name == 'push' && github.event.ref == 'refs/heads/v4'
  datastores:
    - artifact://sun-yryr/Rec-adio
