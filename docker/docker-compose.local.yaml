version: '3'

services:
  scheduler:
    environment:
      - LOG_LEVEL=info
  recorder:
    volumes:
      - ./application/saveData:/app/saveData
    environment:
      - LOG_LEVEL=info
  db:
    ports:
      - "3306:3306"
  redis:
    ports: 
      - '6379:6379'
