# Recoto デーモンの設定ファイル

# ログ設定
[log]
level = "info"  # debug, info, warn, error のいずれか

# サーバー設定
[server]
port = 8080

# gRPC 設定
[grpc]
host = "127.0.0.1"
port = 50051
reflection = true

# 録音設定
[recording]
save_dir = "./data/output"
